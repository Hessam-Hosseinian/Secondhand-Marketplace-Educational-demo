services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      APP_DB_PATH: /app/data/secondhand.db
      APP_UPLOAD_DIR: /app/data/uploads
    volumes:
      - ./data:/app/data
